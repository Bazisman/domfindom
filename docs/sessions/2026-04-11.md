# Сессия 2026-04-11

## Что исправили

- Исправлена логика создания будущих повторяющихся операций:
  - если дата транзакции в будущем, создаётся `planned`, а не `actual`;
  - баланс основного счёта не меняется раньше времени;
  - при создании recurring-шаблона больше не появляется дубль в том же месяце.
- Для planned-транзакций добавлены helper-операции в `core.py`:
  - `add_planned_transaction(...)`
  - `assign_template_to_planned_transaction(...)`
- Быстрый ввод с главной страницы улучшен:
  - клик по категории открывает форму суммы;
  - действия `Внести` / `Отмена`;
  - стиль кнопок выровнен по общему шрифту интерфейса (`font: inherit`).

## Техническая проверка

- `python -m unittest tests.test_web_api` -> `OK` (`21` тест).
- `npm run build` -> `OK`.

## Ключевой результат для бага с зарплатой

- Повторяющаяся зарплата на будущую дату теперь не исполняется сразу.
- В день создания не происходит раннего зачисления в баланс.
- На дату исполнения операция корректно остаётся в planned и будет исполнена по расписанию.

## Короткие команды для обновления на хостинге

Локально (Windows):

```powershell
cd E:\Python\finance_app\src
git add backend/api/routes/transactions.py core.py tests/test_web_api.py frontend/src/pages/DashboardPage.tsx frontend/src/styles.css docs/sessions/2026-04-11.md
git commit -m "Fix recurring future-dated transaction logic and quick entry UX"
git push
cd frontend
npm run build
```

На сервере (REG.RU shell):

```bash
cd ~/finance-app
git pull
cp -r ~/finance-app/frontend/dist/* /var/www/u3480024/data/www/domfindom.ru/
touch /var/www/u3480024/data/www/domfindom.ru/.restart-app
curl -L https://domfindom.ru/api/v1/health
```

Ожидаемый ответ health-check:

```json
{"status":"ok"}
```

## Дополнение (UI/деплой, 2026-04-11)

- Главная страница упрощена:
  - убрана карточка API-статуса;
  - убран блок "Бюджетные акценты";
  - быстрый ввод по категориям перенесён выше списка последних транзакций для более быстрого доступа.
- Улучшена мобильная навигация:
  - при перезагрузке страницы верхняя полоска вкладок автоскроллится к активной вкладке.
- Проведена серия правок мобильной вёрстки:
  - проблемные секции в "Планировании" переведены в безопасный одноколоночный поток;
  - усилены ограничения сетки, чтобы карточки не "наезжали" друг на друга;
  - стабилизировано поведение полей даты в карточках.
- Для полей даты добавлены улучшения UX:
  - единая обёртка `date-shell`;
  - выравнивание текста даты по левому краю;
  - добавлена иконка календаря справа в поле.
- Проверка backend на домене:
  - `curl -sS -L https://domfindom.ru/api/v1/health` -> `{"status":"ok"}`.

### Короткий сценарий обновления (рабочий)

Локально (Windows):

```powershell
cd E:\Python\finance_app\src\frontend
npm run build
```

На сервер:

```powershell
scp -r .\dist\* u3480024@31.31.198.54:/var/www/u3480024/data/www/domfindom.ru/
ssh reg-finance "touch /var/www/u3480024/data/www/domfindom.ru/.restart-app"
ssh reg-finance "curl -sS -L https://domfindom.ru/api/v1/health; echo"
```

## Дополнение (анимации + SSH-ключ, 2026-04-11)

- UI/UX:
  - добавлены более мягкие анимации и переходы для карточек, кнопок, чипов и полей;
  - усилена плавность hover-эффектов (без резкого появления тени);
  - для форм добавлено мягкое появление (`rise-in`);
  - оставлена поддержка `prefers-reduced-motion` (анимации отключаются при системной настройке).
- Поля даты:
  - сохранён кастомный стиль поля даты (`date-shell` + иконка календаря);
  - для desktop добавлено принудительное открытие нативного пикера по клику через `showPicker()`;
  - исправлены сценарии, где календарь открывался нестабильно.
- Деплой и доступ:
  - создан отдельный SSH-ключ для деплоя: `~/.ssh/reg_finance_deploy`;
  - публичный ключ добавлен на сервер в `~/.ssh/authorized_keys`;
  - в `~/.ssh/config` добавлен хост `reg-finance` с `IdentityFile ~/.ssh/reg_finance_deploy` и `IdentitiesOnly yes`;
  - подтвержден вход без пароля: `ssh reg-finance "whoami && pwd"` -> `u3480024 /var/www/u3480024/data`.
- Результат:
  - фронтенд успешно загружен через `scp` с alias `reg-finance`;
  - backend health-check успешен: `{"status":"ok"}`.

## Дополнение (мобильная вкладка "Счета", 2026-04-11)

- На мобильных устройствах изменен порядок секций во вкладке `Счета`:
  - первой показывается секция `Мои деньги`;
  - формы (`Автоотчисления`, `Новый счёт капитала`, `Перевод между счетами`) идут ниже.
- В карточке `Мои деньги` на телефоне изменена компоновка:
  - `Основной счёт` расположен сверху;
  - `Весь капитал` расположен ниже (одна колонка, без бокового расположения).
- Дополнительно:
  - смягчены hover-анимации карточек (более плавная тень и подъем);
  - подтверждена успешная сборка фронтенда после правок.

## Дополнение (план авторизации, 2026-04-11)

### Что сделано
- Прочитана и сверена документация проекта (`docs/*`, `KODA.md`, `TASKS.md`, текущие сессии) с фактическим кодом (`backend/`, `frontend/`, `core.py`).
- Подготовлен конкретный поэтапный план внедрения авторизации и персонализации:
  - файл: `docs/AUTH_IMPLEMENTATION_PLAN.md`.

### Ключевой вывод по архитектуре
- Для текущей кодовой базы безопаснее внедрять авторизацию через:
  - отдельную БД идентификации/сессий (`auth.db`);
  - отдельную финансовую БД на пользователя (per-user DB routing в API).
- Такой путь минимизирует риск регрессий в `core.py`, где сейчас много single-user допущений (`accounts.id = 1` и т.д.).

### Что зафиксировано в рабочем контуре
- Обновлён `TASKS.md`: авторизация и data isolation подняты в ближайший приоритет.
- Обновлён `docs/PROJECT_STATE.md`: добавлен блок о готовом auth-roadmap и следующем шаге реализации.

### Следующий шаг
- Реализация `Этапа 1`:
  - `register/login/logout/me`;
  - хранение пользователей и сессий;
  - backend-тесты авторизации.

## Supplement (auth implementation + hardening, 2026-04-11)

Done:
- Implemented backend auth endpoints: `/api/v1/auth/register`, `/login`, `/logout`, `/me`.
- Implemented per-user DB isolation for all private API requests.
- Added login page, protected routing, and logout UX in frontend.
- Added password strength checks on registration.
- Added session cleanup and optimized user DB bootstrap (no heavy re-init on each request).
- Added production guard for secret management (`FINANCE_APP_ENV=production` requires custom `FINANCE_APP_SESSION_SECRET`).

Verification:
- `python -m unittest tests.test_financial_logic tests.test_web_api -v`
- `npm run build`

### Update 2026-04-11 (rate limit + audit)
- Added backend login rate limiting with configurable window/attempts.
- Added auth event audit table and event logging for register/login/logout and blocked attempts.
- Added test coverage for login rate limiting.

### Update 2026-04-11 (password change + reset)
- Implemented backend endpoint `POST /api/v1/auth/change-password`.
- Implemented password reset request/confirm endpoints and one-time reset tokens with expiry.
- Added test coverage for password change and password reset end-to-end flows.
- Validation: `python -m unittest tests.test_financial_logic tests.test_web_api -v` and `npm run build` are green.

### Update 2026-04-11 (frontend auth UX)
- Added "Forgot password" flow to login page (`request` + `confirm` token).
- Added dedicated protected page `Безопасность` with change-password form.
- Added new route `/security` and top navigation link.
- Validation: `npm run build` is green.

### Update 2026-04-11 (SMTP for reset emails)
- Added `backend/auth/mailer.py` with SMTP sender for password reset emails.
- Added env-configurable SMTP settings and reset URL template.
- `POST /api/v1/auth/password-reset/request` now sends email when SMTP is configured.
- Backend regression check: `python -m unittest tests.test_web_api -v` is green.
