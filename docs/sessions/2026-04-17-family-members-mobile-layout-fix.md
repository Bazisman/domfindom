# 2026-04-17 - Family members card mobile layout fix

## Что исправлено
- На телефоне карточка `Участники семьи` больше не ломает селект роли и кнопку действия.
- Управляющие элементы участника семьи теперь складываются в колонку на узком экране.
- Кнопка и селект растягиваются по ширине карточки и не вылезают за границы.

## Измененный файл
- `frontend/src/styles.css`

## Проверка
- `python tools/check_encoding.py --root .`
- `npm run build`
