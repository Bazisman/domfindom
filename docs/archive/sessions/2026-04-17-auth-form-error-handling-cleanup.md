# 2026-04-17 - Auth form error handling cleanup

## Что исправлено
- Убраны сырые сообщения вида `API error 500` и `API error 422` из окна авторизации.
- Добавлена клиентская проверка email и пароля до отправки запроса.
- Для регистрации и сброса пароля пароль теперь сразу проверяется на длину, заглавные и строчные буквы, цифры.
- Ошибки сети и временные ошибки сервера теперь показываются понятными сообщениями на русском языке.
- Ошибки валидации FastAPI/Pydantic теперь читаются и превращаются в пользовательские подсказки.

## Измененные файлы
- `frontend/src/lib/api.ts`
- `frontend/src/pages/LoginPage.tsx`

## Проверка
- `python tools/check_encoding.py --root .`
- `npm run build`
