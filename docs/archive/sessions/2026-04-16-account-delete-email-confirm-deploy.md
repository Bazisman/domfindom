# Протокол: выкладка удаления аккаунта через email-подтверждение
Дата: 2026-04-16

## Контекст
- Запрос: выкатить в прод механизм удаления аккаунта через письмо с одноразовой ссылкой (токен).
- Дополнительно: зафиксировать результат в журнале.

## Что сделано
1. Код и тесты
- Подготовлены backend/frontend изменения:
  - `POST /api/v1/account/delete/request`
  - `POST /api/v1/auth/account-delete/confirm`
  - отправка письма на удаление аккаунта
  - UI-кнопка в меню аккаунта + подтверждение
- Добавлены тесты в `tests/test_web_api.py` для сценария удаления.

2. Локальная проверка
- `python tools/check_encoding.py --root .` -> `Encoding check passed`.
- `npm run build` (frontend) -> успешно.
- Точечные тесты удаления аккаунта -> `OK`.

3. Git
- Коммит: `e9232c8`
- Сообщение: `Add email-confirmed account deletion flow`
- Push: `origin/main` успешно.

4. Хостинг
- Код подтянут в `/var/www/u3480024/data/finance-app`.
- На хосте frontend build упал из-за python-версии (`from __future__ import annotations` в `tools/check_encoding.py`).
- Применён безопасный обход:
  - локально собран `frontend/dist`
  - архив загружен на хост
  - `dist` развернут вручную
  - приложение перезапущено.
- Проверка: `https://domfindom.ru/api/v1/health` -> `{"status":"ok"}`.

## Замечание по проверке API через curl
- `GET /api/v1/account/delete/request` возвращает `index.html` (SPA fallback), это ожидаемо для не-API метода/маршрута в браузерном GET.
- Проверять нужно из приложения (авторизованный `POST`) или через корректный API-вызов с методом `POST`.

## Итоговый статус
- Выкладка выполнена.
- Функционал удаления аккаунта с подтверждением по email доставлен в прод-код и прод-статические файлы.
- Сервис в рабочем состоянии (health green).
