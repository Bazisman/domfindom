# 2026-04-17 - Family members button text and layout fix

## Что исправлено
- В карточке `Участники семьи` на телефоне восстановлен нормальный текст кнопки `Исключить`.
- Во время действия кнопка показывает `Исключаем...`.
- Для карточек участников добавлена отдельная мобильная раскладка, независимая от общего `.list-item`.
- На узком экране карточка участника семьи теперь собирается в колонку и не съезжает.

## Измененные файлы
- `frontend/src/pages/FamilyPage.tsx`
- `frontend/src/styles.css`

## Проверка
- `python tools/check_encoding.py --root .`
- `npm run build`
